# @param {Integer[]} nums
# @return {Integer[]}
def sort_array_by_parity(nums)
    array = []
    nums.each do |num|
        array << num if num.even?    
    end
    nums.each do |num|
        array << num if num.odd?  
    end
    array
end